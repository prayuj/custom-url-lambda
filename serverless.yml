service: custom-url
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev

plugins:
  - serverless-offline

functions:
  hello:
    handler: myService/handler.hello
    events:
      - http:
          path: /hello
          method: GET
